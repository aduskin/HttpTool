<AduSkin:AduWindow
    x:Class="HttpTool.Desktop.Views.CodeGeneratorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:AduAttach="clr-namespace:AduSkin.AdditionalAttributes;assembly=AduSkin"
    xmlns:AduSkin="clr-namespace:AduSkin.Controls;assembly=AduSkin"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:HttpTool.Desktop.ViewModels"
    Title="Generate Code"
    Width="700"
    Height="500"
    MinWidth="500"
    MinHeight="400"
    AduAttach:HeaderAttach.Background="{DynamicResource NormalTransparentBrush}"
    AduAttach:HeaderAttach.FontSize="14"
    AduAttach:HeaderAttach.Foreground="{DynamicResource NormalForeground}"
    AduAttach:HeaderAttach.Height="40"
    AduAttach:HeaderAttach.IconWidth="20"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Language Selection  -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Margin="0,0,0,8" Text="Select Language:" />
            <AduSkin:AduComboBox
                DisplayMemberPath="DisplayName"
                ItemsSource="{Binding SupportedLanguages}"
                SelectedItem="{Binding SelectedLanguage}" />
        </StackPanel>

        <!--  Code Display  -->
        <AduSkin:AduTextBox
            Grid.Row="1"
            Height="Auto"
            Padding="4"
            VerticalAlignment="Stretch"
            VerticalContentAlignment="Top"
            AcceptsReturn="True"
            FontFamily="Consolas"
            FontSize="12"
            HorizontalScrollBarVisibility="Auto"
            IsReadOnly="True"
            Text="{Binding GeneratedCode, Mode=OneWay}"
            VerticalScrollBarVisibility="Auto" />

        <!--  Buttons  -->
        <StackPanel
            Grid.Row="2"
            Margin="0,12,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <AduSkin:AduButton
                Width="80"
                Margin="0,0,8,0"
                Command="{Binding CopyCommand}"
                Content="Copy" />
            <AduSkin:AduButton
                Width="80"
                Command="{Binding CloseCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=AduSkin:AduWindow}}"
                Content="Close" />
        </StackPanel>
    </Grid>
</AduSkin:AduWindow>
